# ADAMO Content Studio

1. Copy `.env.example` to `.env` and set the Sanity project ID.
2. Run `npm install`.
3. Run `npm run seed` once.
4. Run `npm run dev` locally or `npm run deploy` for the hosted Studio.

Use Node.js 22 LTS or 24 LTS.
